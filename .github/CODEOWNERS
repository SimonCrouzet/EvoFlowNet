# Every pull request requests a review from the owner listed here.
# Paths are matched last-wins, so later rules override earlier ones.

* @SimonCrouzet

# Infrastructure that can publish, grant permissions, or run with elevated
# tokens. Called out separately so a change here is never skimmed as routine.
/.github/workflows/ @SimonCrouzet
/pyproject.toml     @SimonCrouzet
/uv.lock            @SimonCrouzet
