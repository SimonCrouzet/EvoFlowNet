_target_: evoflownet.algorithms.gflownet.training.TrainingConfig
steps: 2000
batch_size: 64
learning_rate: 3.0e-3
# logZ must travel to log sum R(x); at the policy's rate it is the bottleneck.
log_z_multiplier: 10.0
epsilon_start: 0.3
epsilon_end: 0.0
log_every: 50
seed: 0
